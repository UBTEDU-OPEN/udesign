import { Locale } from '../interface';

const local: Locale = {
  code: 'en-US',
  Pagination: {
    item: 'item',
    pageSize: ' items / page',
    page: ' pages',
    total: '',
    jumpTo: 'Jump to',
  },
  Modal: {
    confirm: 'Confirm',
    cancel: 'Cancel',
  },
  Empty: {
    description: 'No Result',
  },
};

export default local;
