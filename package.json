{
  "name": "udesign",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "app/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run --parallel build",
    "lint": "lerna run --parallel lint"
  },
  "devDependencies": {
    "typescript": "4.5.5",
    "lerna": "^4.0.0"
  }
}
